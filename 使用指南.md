# Nona 论文下载工具 - 详细使用指南

## 📋 目录
1. [配置说明](#配置说明)
2. [快速开始](#快速开始)
3. [不同领域论文下载方法](#不同领域论文下载方法)
4. [完整工作流程](#完整工作流程)
5. [常用命令速查](#常用命令速查)
6. [常见问题](#常见问题)

---

## 🔧 配置说明

### 1. 环境配置

项目已经自动创建了 `.env` 文件，你需要编辑它来配置 LLM API。

#### 编辑配置文件

```bash
# 使用你喜欢的编辑器打开 .env 文件
nano .env
# 或
vim .env
# 或使用 VS Code
code .env
```

#### 推荐配置方案

**方案1：使用 OpenRouter（推荐，性价比高）**

```bash
DEFAULT_LLM_PROVIDER=custom
LLM_API_KEY=sk-or-v1-your-key-here
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=deepseek/deepseek-chat
```

获取密钥：https://openrouter.ai/keys

**方案2：使用 DeepSeek（性价比高，约¥1/百万tokens）**

```bash
DEFAULT_LLM_PROVIDER=custom
LLM_API_KEY=sk-your-deepseek-key
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
```

获取密钥：https://platform.deepseek.com

**方案3：使用 Google Gemini（免费额度大）**

```bash
DEFAULT_LLM_PROVIDER=custom
LLM_API_KEY=your-google-api-key
LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
LLM_MODEL=gemini-2.0-flash-exp
```

获取密钥：https://aistudio.google.com/apikey

**方案4：使用 OpenAI（官方）**

```bash
DEFAULT_LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-key
OPENAI_BASE_URL=https://api.openai.com/v1
DEFAULT_OPENAI_MODEL=gpt-4o-mini
```

### 2. 测试配置

配置完成后，运行测试脚本验证配置是否正确：

```bash
python tests/test_llm_config.py
```

如果看到 "✓ LLM配置测试通过"，说明配置成功！

---

## 🚀 快速开始

### 最简单的使用流程（3步）

```bash
# 1. 收集论文标题（例如：下载2025年计算机视觉领域的论文）
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 100

# 2. 获取论文详细信息并下载PDF（限制10篇用于测试）
python scripts/fetch_paper_info.py --limit 10
python scripts/process_pdfs.py --limit 10

# 3. 查看结果
python scripts/quick_verify.py
```

---

## 📚 不同领域论文下载方法

### arXiv 论文分类说明

arXiv 使用分类代码来组织论文，常用分类如下：

#### 计算机科学类（cs.*）
- `cs.AI` - 人工智能
- `cs.CV` - 计算机视觉
- `cs.CL` - 计算语言学
- `cs.LG` - 机器学习
- `cs.RO` - 机器人学
- `cs.NE` - 神经网络与进化计算
- `cs.SY` - 系统与控制
- `cs.IT` - 信息论
- `cs.DS` - 数据结构与算法
- `cs.SE` - 软件工程

#### 其他学科类
- `math.OC` - 优化与控制
- `math.ST` - 统计学理论
- `stat.ML` - 统计机器学习
- `q-bio.QM` - 定量生物学
- `physics` - 物理学

### 下载不同领域论文的示例

#### 1. 计算机视觉（Computer Vision）

```bash
# 收集2025年计算机视觉论文（最多5000篇）
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 5000

# 获取详细信息（限制100篇）
python scripts/fetch_paper_info.py --limit 100

# 下载PDF（限制50篇）
python scripts/process_pdfs.py --limit 50
```

#### 2. 自然语言处理（NLP）

```bash
# 收集2025年NLP论文
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CL --max-results 3000

# 获取详细信息并下载
python scripts/fetch_paper_info.py --limit 100
python scripts/process_pdfs.py --limit 50
```

#### 3. 机器学习（Machine Learning）

```bash
# 收集2025年机器学习论文
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.LG --max-results 5000

# 获取详细信息并下载
python scripts/fetch_paper_info.py --limit 100
python scripts/process_pdfs.py --limit 50
```

#### 4. 人工智能（AI）

```bash
# 收集2025年AI论文
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.AI --max-results 3000

# 获取详细信息并下载
python scripts/fetch_paper_info.py --limit 100
python scripts/process_pdfs.py --limit 50
```

#### 5. 机器人学（Robotics）

```bash
# 收集2025年机器人学论文（默认分类）
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.RO --max-results 3000

# 获取详细信息并下载
python scripts/fetch_paper_info.py --limit 100
python scripts/process_pdfs.py --limit 50
```

#### 6. 多个分类组合（需要手动修改代码或多次运行）

如果你想同时收集多个分类，可以多次运行：

```bash
# 收集计算机视觉
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 2000

# 收集自然语言处理
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CL --max-results 2000

# 收集机器学习
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.LG --max-results 2000
```

数据库会自动去重，所以不用担心重复。

### 顶级会议论文

除了 arXiv，你还可以收集顶级会议的论文：

#### NeurIPS（神经信息处理系统）

```bash
python scripts/collect_titles.py --source neurips --year 2024
```

#### ICLR（国际学习表征会议）

```bash
python scripts/collect_titles.py --source iclr --year 2025
```

#### ICML（国际机器学习会议）

```bash
python scripts/collect_titles.py --source icml --year 2024
```

#### CoRL（机器人学习会议）

```bash
python scripts/collect_titles.py --source corl --year 2024
```

#### RSS（机器人科学系统会议）

```bash
python scripts/collect_titles.py --source rss --year 2024
```

#### ICRA（国际机器人与自动化会议）

```bash
python scripts/collect_titles.py --source icra --year 2024
```

#### IROS（智能机器人与系统国际会议）

```bash
python scripts/collect_titles.py --source iros --year 2024
```

#### 收集所有来源

```bash
# 收集所有会议和arXiv（默认cs.RO分类）
python scripts/collect_titles.py --source all --year 2024 --max-results 6000
```

---

## 🔄 完整工作流程

### 标准工作流程（4步）

```bash
# ========== 步骤1: 收集论文标题 ==========
# 根据你的需求选择来源和分类
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 3000

# ========== 步骤2: 获取论文详细信息 ==========
# 获取arXiv ID、PDF链接、作者、摘要等信息
# 建议先用小数量测试，避免API超额
python scripts/fetch_paper_info.py --limit 100

# ========== 步骤3: 下载并处理PDF ==========
# 下载PDF并转换为文本
# 建议先用小数量测试，节省空间和时间
python scripts/process_pdfs.py --limit 50

# ========== 步骤4: AI分析论文相关性（可选） ==========
# 使用LLM分析论文与你的研究领域的相关性
# 需要配置LLM API才能使用
python scripts/analyze_papers.py --limit 20 --min-score 0.7
```

### 批量处理工作流程

如果你想处理大量论文，可以分批次进行：

```bash
# 1. 收集大量论文标题
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 10000

# 2. 分批获取详细信息（每次100篇）
python scripts/fetch_paper_info.py --limit 100
# 等待一段时间后再次运行，直到处理完所有论文
python scripts/fetch_paper_info.py --limit 100
# ... 重复直到没有待处理的论文

# 3. 分批下载PDF（每次50篇）
python scripts/process_pdfs.py --limit 50
# 等待一段时间后再次运行
python scripts/process_pdfs.py --limit 50
# ... 重复直到下载完所有论文

# 4. 分批分析（每次20篇，控制成本）
python scripts/analyze_papers.py --limit 20
# ... 重复直到分析完所有论文
```

---

## 📖 常用命令速查

### 收集论文

```bash
# arXiv - 指定分类
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 3000

# 单个会议
python scripts/collect_titles.py --source neurips --year 2024

# 所有来源
python scripts/collect_titles.py --source all --year 2024
```

### 获取论文信息

```bash
# 处理所有待处理的论文
python scripts/fetch_paper_info.py

# 限制处理数量
python scripts/fetch_paper_info.py --limit 100

# 处理指定状态的论文
python scripts/fetch_paper_info.py --status pendingTitles --limit 50
```

### 下载和处理PDF

```bash
# 下载并转换PDF
python scripts/process_pdfs.py

# 限制处理数量
python scripts/process_pdfs.py --limit 50

# 只下载，不转换文本
python scripts/process_pdfs.py --skip-convert --limit 100

# 只转换文本，不下载（适用于已下载的PDF）
python scripts/process_pdfs.py --skip-download
```

### AI分析

```bash
# 分析论文相关性
python scripts/analyze_papers.py

# 使用特定LLM提供商
python scripts/analyze_papers.py --provider custom

# 设置最小相关性分数
python scripts/analyze_papers.py --min-score 0.7 --limit 20
```

### 数据管理

```bash
# 查看数据库统计
python scripts/quick_verify.py

# 重试失败的详情获取
python scripts/retry_failures.py --type detail

# 重试失败的下载
python scripts/retry_failures.py --type download

# 重置指定论文
python scripts/reset_paper.py --paper-id <ID> --clear-info --delete-files

# 清空所有数据（谨慎操作）
python scripts/clean_data.py
```

### Web可视化

```bash
# 启动Web界面
uvicorn web.app:app --reload

# 然后在浏览器打开 http://127.0.0.1:8000
```

---

## ❓ 常见问题

### Q1: 如何查看已收集的论文数量？

```bash
python scripts/quick_verify.py
```

### Q2: 下载失败怎么办？

下载失败是正常现象，系统会自动记录失败信息。你可以：

```bash
# 查看失败记录
python scripts/quick_verify.py

# 重试失败的下载
python scripts/retry_failures.py --type download
```

### Q3: 如何只下载特定领域的论文？

使用 `--arxiv-category` 参数指定分类：

```bash
# 计算机视觉
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV

# 自然语言处理
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CL

# 机器学习
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.LG
```

### Q4: 论文存储在哪里？

- 数据库：`data/papers.db`
- PDF文件：`data/pdfs/`
- 文本文件：`data/texts/`

### Q5: 如何自定义筛选关键词？

编辑 `config.py` 文件中的 `RELEVANCE_TAGS` 列表：

```python
RELEVANCE_TAGS = [
    "你的关键词1",
    "你的关键词2",
    # 添加更多关键词
]
```

### Q6: 可以同时处理多个分类吗？

可以！多次运行收集命令，数据库会自动去重：

```bash
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CV --max-results 2000
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.CL --max-results 2000
python scripts/collect_titles.py --source arxiv --year 2025 --arxiv-category cs.LG --max-results 2000
```

### Q7: LLM API配置失败怎么办？

1. 检查 `.env` 文件中的API密钥是否正确
2. 运行测试：`python tests/test_llm_config.py`
3. 查看 `env.example` 文件中的配置示例
4. 确保网络连接正常

### Q8: 如何查看论文的详细信息？

使用Web界面：

```bash
uvicorn web.app:app --reload
```

然后在浏览器打开 http://127.0.0.1:8000

### Q9: 论文状态是什么意思？

- `pendingTitles` - 已收集标题，等待获取详细信息
- `TobeDownloaded` - 已获取信息，等待下载PDF
- `processed` - PDF已下载并转换为文本
- `analyzed` - 已完成AI分析
- `detailFailed` - 获取详细信息失败
- `downloadFailed` - PDF下载失败

### Q10: 如何批量重置论文状态？

```bash
# 重置所有下载，回退为pending
python scripts/reset_all_downloads.py

# 将下载失败的条目重新入队
python scripts/requeue_download_failures.py --clear-info --delete-files
```

---

## 📝 论文状态流程

```
pendingTitles → TobeDownloaded → processed → analyzed
     ↓              ↓
detailFailed   downloadFailed
```

---

## 🎯 最佳实践

1. **先小批量测试**：先用 `--limit 10` 测试流程是否正常
2. **分批处理**：大量论文时，分批处理避免API限制和存储问题
3. **定期检查**：使用 `quick_verify.py` 查看处理进度
4. **重试失败**：定期运行 `retry_failures.py` 重试失败的论文
5. **备份数据**：定期备份 `data/` 目录

---

## 📞 获取帮助

- 查看项目 README：`README.md`
- 查看设计文档：`docs/设计文档.md`
- 查看LLM配置说明：`docs/LLM配置说明.md`
- 运行测试：`python tests/run_all_tests.py`

---

**祝你使用愉快！** 🎉
